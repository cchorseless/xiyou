import React from "react";
import { CSSHelper } from "../../../helper/CSSHelper";
import { CustomAbilityButton_UI } from "./CustomAbilityButton_UI";

export class CustomAbilityButton extends CustomAbilityButton_UI {
    constructor(p: any) {
        super(p);
    }
    // 初始化数据
    componentDidMount() {
        super.componentDidMount();
      
    }
    onStartUI() {
       
    }
}
