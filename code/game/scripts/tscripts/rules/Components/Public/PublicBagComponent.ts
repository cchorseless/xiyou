import { reloadable } from "../../../GameCache";
import { ET } from "../../Entity/Entity";

@reloadable
export class PublicBagComponent extends ET.Component {
    onAwake(...args: any[]): void {

    }
}